import React, { Component } from 'react';
import './Button.css';

class Button extends Component {
  render() {
    return (
        <button name="{this.props.name}" onClick="">{this.props.label}</button>
    );
  }
}

export default Button; // Don’t forget to use export default!
